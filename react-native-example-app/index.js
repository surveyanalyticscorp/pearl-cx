/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';

// Use the package name 
const appName = 'InterceptSDKExample';

AppRegistry.registerComponent(appName, () => App);
